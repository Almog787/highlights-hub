name: Generate README
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Generate README File
        run: |
          echo "# 🌍 Global News Network (GNN)" > README.md
          echo "" >> README.md
          echo "## 🔗 Live Site" >> README.md
          echo "Check out the live news hub here: [https://almog787.github.io/highlights-hub/](https://almog787.github.io/highlights-hub/)" >> README.md
          echo "" >> README.md
          echo "## 📊 Automated Site Status" >> README.md
          echo "* **Last Deployment:** $(date -u)" >> README.md
          echo "* **Status:** Active 🟢" >> README.md
          echo "* **Platform:** GitHub Pages" >> README.md
          echo "" >> README.md
          echo "## 🚀 Project Overview" >> README.md
          echo "This is an automated international news hub optimized for Google Discover and Google News. It fetches live updates globally without requiring a manual backend." >> README.md
          echo "" >> README.md
          echo "### 🛠 Features & SEO" >> README.md
          echo "- **Real-time Feeds:** Automated news updates." >> README.md
          echo "- **Google Search Console Ready:** Integrated sitemap and schema." >> README.md
          echo "- **Analytics:** Google Tag Manager ID \`G-8NEMWLX7BF\`." >> README.md
          echo "- **AdSense:** Pre-configured for monetization." >> README.md
          echo "" >> README.md
          echo "## 📈 Technical Setup" >> README.md
          echo "The project is a static web application built with HTML, Bootstrap 5, and JavaScript. It uses GitHub Actions to maintain documentation and site health." >> README.md
          echo "" >> README.md
          echo "---" >> README.md
          echo "*Generated automatically by GitHub Actions for almog787*" >> README.md

      - name: Commit and Push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add README.md
          git commit -m "docs: automated README update with site link" || exit 0
          git push
